# Krowds PWA

The Krowds progressive web app with a manifest and offline app shell.

```bash
pnpm dev
pnpm build
pnpm start
pnpm lint
pnpm typecheck
```

Development: http://localhost:3004

The service worker runs only in production mode:

```bash
pnpm build
pnpm start
```
